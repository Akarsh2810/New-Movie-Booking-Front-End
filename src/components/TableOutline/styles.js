import { makeStyles } from "@material-ui/core";

const useStyles = makeStyles(() => ({
    layout: {
        // margin: "2em",
        // border: "2px solid green"
    },
    paper: {
        width: "70%",
    }
}))

export {useStyles};