import { Formik, Form } from "formik";

const EmailIdPassword = (props) => {

    return (
        <>
            <Formik>
                {(formik) => (
                    <Form>
                        
                    </Form>
                )}
            </Formik>
        </>
    )
}

export default EmailIdPassword;